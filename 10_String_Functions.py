quote = "Even for the wrong reason we do the right things, right things will happen."
#String function
print(len(quote)) #len function used to fine number of characters
print(quote.endswith("happen.")) #.endswith used to check last character
print(quote.count("r")) #.count used to count of character or also in words
print(quote.replace("happen", "happen to you")) #replace every old words with new words
